Change the product condition values from ('new','used','refurbished') to ('new','used','OEM'). Update condition in both createSellerProductSchema and updateSellerProductSchema in seller-products.validator.js. Add a new sequential migration in src/db/migrations/ that alters the products table condition ENUM to ('new','used','OEM'). Check for and handle any existing 'refurbished' rows. Also check whether any other validators, constants, or seed files reference 'refurbished' and update them for consistency.